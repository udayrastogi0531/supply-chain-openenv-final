Repository commands

Install with `uv sync`, run inference with `uv run inference`, start the server with `uv run server --host 0.0.0.0 --port 7860`, validate with the repository validation script, and test with pytest.